# SaltyGacha

Single-file 8-bit fantasy gacha simulator for drop-rate testing, item pools, spending insight, free rolls, pity, Monte Carlo Player POV, and CSV export.

Open `index.html` or `gacha-simulator.html` to run the simulator.
